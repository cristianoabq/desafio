import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.gs3.desafio',
  appName: 'desafio',
  webDir: 'www',
  bundledWebRuntime: false
 
};

export default config;
